@model Shop.Models.AdminViewModels.ItemsOverviewViewModel

@{
    ViewData["Title"] = "Những mặt hàng ngừng kinh doanh";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<section class="wrapper">
    @if (Model.AllItemsSortedById == null || !Model.AllItemsSortedById.Any())
    {
        <h1 class="text-center mtb-40">Hiện tại không có sản phẩm ngừng kinh doanh trong hệ thống!</h1>
    }
    else
    {
        <div class="text-center">
            <h1><i class="fas fa-search"></i> Tìm kiếm theo tên</h1>
            <input onkeyup="searchInTableFunction()" type="text" class="ggg max-width-500px" id="searchInTableTextField" placeholder="Nhập tên sản phẩm">
        </div>
        <div class="table-agile-info">
            <div class="panel panel-default">
                <div class="panel-heading">
                    Những sản phẩm đã ngừng kinh doanh trên TH-Phone
                </div>
                <div>
                    <table class="table" id="tableToSearch">
                        <thead>
                            <tr>
                                <th>Tên mặt hàng</th>
                                <th>Tên người bán</th>
                                <th>Số lượng đã bán</th>
                                <th>Chi tiét</th>
                            </tr>
                        </thead>
                        @foreach (var items in Model.AllItemsSortedById)
                        {
                            <tbody>
                                <tr>
                                    <td>@items.ItemsName</td>
                                    <td>@items.Name</td>
                                    <td>@items.NumberOfItemsInSystem</td>
                                    <td><a asp-controller="Admin" asp-action="ItemsEdit" asp-route-id="@items.Id"><i class="fas fas fa-ellipsis-h"></i></a></td>
                                </tr>
                            </tbody>
                        }
                    </table>
                </div>
            </div>
        </div>
    }
</section>